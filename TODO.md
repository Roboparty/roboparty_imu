# TODO

## CAN 回调机制

- [x] 支持同一个 CAN 路由 key 注册多个回调。
- [x] 注册时返回唯一 token，注销时使用 `id + token` 精确删除订阅，避免不同 IMU 或驱动互相覆盖、误删回调。
- [x] 将手工管理的 `id + token` 升级为 RAII Subscription，由订阅对象在析构时自动注销。
- [x] 注销订阅时禁止新回调进入并等待已经开始执行的回调结束，避免驱动析构与接收线程并发导致 use-after-free。
- [ ] 明确禁止在回调内部同步注销自身，或为该场景增加无死锁的延迟注销机制。
- [ ] 增加不依赖真实 CAN 设备的回调测试，覆盖同 key 多订阅、独立注销和并发注销。

## 时间戳

- [ ] MCT7123 直接保存并返回原始 64 位微秒时间戳，避免转换为 32 位毫秒后丢失精度并在约 49.7 天后回绕。

## 测试工具

- [ ] `test_imu.py` 根据传感器帧或 cycle/timestamp 变化统计接收速率，不再把 getter 调用次数当作传感器帧数。
- [ ] 未收到有效数据时报告失败，避免设备只成功打开就显示 `OK`。

## 构建与发布

- [ ] CMake 不要强制启用 `ccache`；仅在检测到命令存在时使用，并允许调用方指定 compiler launcher。
- [ ] 不要强制覆盖 `CMAKE_BUILD_TYPE`，保证 Debug 等外部构建配置生效。
- [ ] 检查并补充 ROS/ament 导出的 `mct7123_imu` 目标。
